"""Domain models / dataclasses for the login system.

Layer: domain (the "models" layer in the target architecture).

NOTE: This layer is named ``domain`` rather than ``models`` on purpose. The
repository ``.gitignore`` ignores every ``models/`` directory (used for large
ML weights), which would make a ``Login_system/models`` package untracked.
Using ``domain`` keeps the layer under version control and consistent with
``backend/domain``.

Populated during the architectural refactor; empty in Phase 0.
"""
