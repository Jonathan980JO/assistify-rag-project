"""Persistence/data-access layer for the login system.

Layer: data access. Repositories own all SQL access (users, tenants, memberships, etc.).
Populated during the architectural refactor (Phase 2+); empty in Phase 0.
"""
