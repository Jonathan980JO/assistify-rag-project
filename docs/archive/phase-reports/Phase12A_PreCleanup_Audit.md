# Phase 12A - Pre-Cleanup Repository Safety Audit

- **Phase:** 12A - Final Safe Cleanup Execution (Step 1: Audit)
- **Repository:** `assistify-rag-project-main`
- **Branch:** `refactor/architecture-v2`
- **Date:** 2026-06-27
- **Mode:** ANALYSIS ONLY. No files were moved, deleted, or modified in this step. This document records the observed pre-cleanup state so every later action is backed by evidence.
- **HEAD at audit time:** `da0dc71f` (`phase11b-report`)

---

## 0. Scope and Constraints

This phase is strictly structural. The following are explicitly out of scope and were NOT touched: backend logic, `Login_system` logic, RAG retrieval logic, frontend source, databases (except read-only analysis), Chroma collections, and runtime configuration.

---

## 1. Current Root Folders

Observed via `dir /b /ad` at repository root:

| Folder | Notes |
|---|---|
| `.claude` | tooling config |
| `.git` | VCS metadata |
| `.planning` | tooling/planning metadata |
| `.remember` | tooling metadata |
| `.vscode` | editor config |
| `archive` | created in Phase 10A (audits) |
| `archived_pdfs` | document storage |
| `assistify-rag-project-main` | **NESTED ACCIDENTAL COPY** - see Section 3 (Step 2 target) |
| `assistify-ui-design` | canonical frontend (PROTECTED) |
| `backend` | backend application (PROTECTED) |
| `Backup` | heavy assets (deferred per Phase 10A) |
| `chroma_db` | runtime Chroma data (PROTECTED) |
| `chroma_db_production` | runtime Chroma data (PROTECTED) |
| `docs` | documentation; contains `docs/archive/` |
| `graduation` | orphaned/incomplete venv (no `python.exe`); large vendor tree |
| `Json` | data |
| `legacy` | Phase 10A relocation target (`non_functional`, `ui-drafts`, `stubs`) |
| `Login_system` | login application (PROTECTED) |
| `logs` | runtime logs (PROTECTED) |
| `LOGSS` | logs |
| `models` | model assets (PROTECTED) |
| `Notepad_Test_Results` | test outputs |
| `Qwen2.5-7B-GGUF` | large model weights |
| `scratch` | scratch space |
| `scripts` | maintenance/launch/diagnostic scripts |
| `tests` | test suite |
| `tools` | experiments/diagnostics/testing tools |
| `TTS` | TTS assets |
| `TTS_local_backup` | TTS backup |
| `tts_service` | TTS service |
| `xtts_service` | XTTS service |
| `__pycache__` | git-ignored, regenerated by Python at root on import/compile |

## 2. Current Root Files

Observed via `dir /b /a-d` at repository root:

| File | Tracked? | Notes |
|---|---|---|
| `.cursorrules` | tracked | engineering rule |
| `.env` | (ignored) | runtime config - DO NOT TOUCH |
| `.env.example` | tracked | template |
| `.gitignore` | tracked | ignore rules |
| `._assistify_session_secret` | (ignored) | secret - DO NOT TOUCH |
| `AGENT_TASK_PROMPT.md` | tracked | docs |
| `AI_AGENT_RULES.md` | tracked | docs |
| `cleanup_repo.py` | tracked | retained at root (Phase 10A decision) |
| `config.py` | tracked | import root (validation target) |
| `conftest.py` | tracked | pytest root (validation target) |
| `environment_main.yml` | tracked | env spec |
| `environment_main_locked.yml` | tracked | env spec |
| `environment_xtts.yml` | tracked | env spec |
| `environment_xtts_locked.yml` | tracked | env spec |
| `hotswap_validation.py` | tracked | retained at root (Phase 10A decision) |
| `LAUNCHER_README.md` | tracked | docs |
| `Phase10A_Report.md` | tracked | **Step 3 archive target** |
| `Phase11A_Shadow_Module_Report.md` | **untracked** | **Step 3 archive target** (no prior git history) |
| `Phase11B_Deployment_Hardening_Report.md` | tracked | **Step 3 archive target** |
| `README.md` | tracked | docs |
| `repo_tree_after_phase11b.txt` | untracked + **ignored** | generated tree dump; `.gitignore:160 *.txt` |
| `start_main_servers.py` | tracked | launcher (validation target) |
| `start_piper_service.bat` | tracked | launcher |
| `test_list_patch.py` | tracked | retained at root (Phase 10A decision) |
| `test_search.py` | tracked | retained at root (Phase 10A decision) |

`Phase12A_PreCleanup_Audit.md` (this file) is created by Step 1 and committed at root.

## 3. Remaining Duplicate Repositories / Projects

### 3.1 Nested accidental repository copy

Path: `assistify-rag-project-main/` (one level below the real repo root).

Full recursive content (`dir /s /b`):
```
assistify-rag-project-main\backend
assistify-rag-project-main\backend\chroma_db_reindex
assistify-rag-project-main\backend\chroma_db_reindex\chroma.sqlite3
```

Evidence:
- **Not a git repository / submodule:** `if exist assistify-rag-project-main\.git` -> `NO_GIT`.
- **Untracked:** `git ls-files assistify-rag-project-main` -> empty.
- **Git-ignored:** `git check-ignore -v` -> `.gitignore:52:chroma_db_reindex/` matches `assistify-rag-project-main/backend/chroma_db_reindex/chroma.sqlite3`.
- **Single payload:** only `chroma.sqlite3` (~167 KB per Phase 11A audit). No source, no manifests.
- This matches the Phase 11A finding (Section 6): an accidental self-nesting from a mis-rooted reindex/test run.

This is the Step 2 deletion target.

### 3.2 Duplicate UI projects (already handled in Phase 10A)

`assistify-ui-design (1)` and `assistify-ui-design (2)` were relocated to `legacy/ui-drafts/` in Phase 10A. No duplicate UI projects remain at root. Canonical `assistify-ui-design/` is the only active frontend.

### 3.3 Other relocated duplicates

`legacy/` now contains `non_functional/`, `ui-drafts/`, and `stubs/` (Phase 11B). `archive/audits/` holds prior audit snapshots. No further duplicate projects observed at root.

## 4. Generated Artifacts in the Tree

| Artifact | Tracked? | Classification |
|---|---|---|
| `assistify-ui-design/tsconfig.tsbuildinfo` | **tracked (committed)** | generated TypeScript incremental-build cache; committed to git. Flagged for Step 4 frontend audit (analysis only). |
| `assistify-ui-design/.next/` | ignored (`.gitignore:209 .next/`) | Next.js build output - generated/reproducible |
| `assistify-ui-design/out/` | ignored (`.gitignore:210 out/`) | Next.js static export - generated/reproducible |
| `repo_tree_after_phase11b.txt` | ignored (`.gitignore:160 *.txt`) | generated tree dump at root |
| root `__pycache__/` | ignored | regenerated by Python on compile/import |

Only `tsconfig.tsbuildinfo` is a generated artifact actually committed to git. It is left untouched in Phase 12A (Step 4 is analysis-only; no `.gitignore` edits).

## 5. Empty Directories

A full recursive scan for zero-entry directories (excluding `.git/`, the Step 5 protected list `backend/`, `Login_system/`, `assistify-ui-design/`, `models/`, `chroma_db*/`, `logs/`, and heavy vendor/venv trees `graduation/`, `Qwen2.5-7B-GGUF/`, `node_modules`) returned exactly one:

```
legacy/non_functional/old_python
```

This directory is under the git-ignored `legacy/non_functional/` tree, so removing it produces zero tracked git changes. It is the only Step 5 cleanup target.

## 6. Working Tree State

`git status --porcelain` at audit time:
```
?? Phase11A_Shadow_Module_Report.md
```

The only untracked item that affects git status is the Phase 11A report (to be archived in Step 3). `repo_tree_after_phase11b.txt` does not appear because it is ignored by `*.txt`.

## 7. Planned Actions (NOT executed in this step)

| Step | Action | Target | Git impact |
|---|---|---|---|
| 2 | Delete nested copy | `assistify-rag-project-main/` | none (untracked+ignored) |
| 3 | Archive reports | `Phase10A_Report.md`, `Phase11B_Deployment_Hardening_Report.md` (git mv), `Phase11A_Shadow_Module_Report.md` (move+add) -> `docs/archive/` | renames + 1 add |
| 4 | Audit only | `.next/`, `out/`, `tsconfig.tsbuildinfo` | none |
| 5 | Delete empty dir | `legacy/non_functional/old_python` | none (ignored) |
| 6 | Validate + report | `config.py`, `conftest.py`, `start_main_servers.py`, backend/login imports | none |

## 8. Compliance Assessment (per core engineering rule)

1. **Genericity Assessment - PASS.** This audit keys only on content-agnostic signals: directory structure, file type, git tracking status (`git ls-files`), `.gitignore` rules (`git check-ignore`), and filesystem layout. No logic depends on any company, product, document, dataset, metric, or hardcoded value.
2. **Evidence-Origin Assessment - PASS.** Every classification is backed by reproduced command output (directory listings, `git ls-files`, `git check-ignore`, `git status`). Nothing is inferred from known content.
3. **Future-Document Compatibility Assessment - PASS.** Pure inventory of repository structure; no runtime path or content dependency introduced. A brand-new document or domain added tomorrow would be unaffected.

*End of Phase 12A Step 1 audit. No modifications performed.*
